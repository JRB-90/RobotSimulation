namespace JSim.Core
{
    /// <summary>
    /// Enum defining the logging severity level that a message should have.
    /// </summary>
    public enum LogLevel
    {
        Debug,
        Info,
        Warning,
        Error,
        Fatal
    }
}
