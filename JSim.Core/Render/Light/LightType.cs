namespace JSim.Core.Render
{
    /// <summary>
    /// Stores the different supported lighting types.
    /// </summary>
    public enum LightType
    {
        Ambient,
        Directional,
        Point,
        Spot
    }
}
